projects:
  - name: demo-aws-vpc
    dir: projects/aws/vpc
    stacks:
      - west
  - name: demo-aws-vpn
    dir: projects/aws/vpn
    stacks:
      - west
    dependsOn:
      - demo-aws-vpc
  - name: demo-aws-connectors
    dir: projects/aws/connectors
    stacks:
      - west
    dependsOn:
      - demo-aws-vpc
  - name: demo-aws-eks
    dir: projects/aws/eks
    stacks:
      - west
    dependsOn:
      - demo-aws-vpc
  - name: demo-aws-connectors
    dir: projects/aws/connectors
    stacks:
      - west
    dependsOn:
      - demo-aws-vpc
      - demo-aws-eks
  
